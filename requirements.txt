# Root-level requirements for running tests locally.
# Each service has its own requirements.txt used inside Docker.
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
redis>=5.0.0
pydantic>=2.0.0
prometheus-client>=0.20.0
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0
locust>=2.24.0
ruff>=0.4.0
